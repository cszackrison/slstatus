#!/bin/sh

# Show CodexBar's current usage in a small terminal popup.

exec st -c SlstatusPopup -n slstatus-usage -t "AI usage" \
	-g "82x34" \
	-e sh -c 'printf "Loading usage...\n"; slstatus -u; exec tail -f /dev/null'
