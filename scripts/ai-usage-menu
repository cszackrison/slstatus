#!/bin/sh

# Show locally cached AI subscription usage in a small terminal popup.

exec st -c SlstatusPopup -n slstatus-usage -t "AI usage" \
	-g "68x6" \
	-e sh -c 'slstatus -u; exec tail -f /dev/null'
